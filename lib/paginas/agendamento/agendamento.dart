import 'package:flutter/material.dart';

class AgendaHorario extends StatelessWidget {
  const AgendaHorario({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Quadras Sport"),
      ),
      
    );
  }
}
